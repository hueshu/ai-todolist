# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# AI Configuration
# OpenAI (可选，如果使用 OpenAI)
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gpt-4o  # 可选，默认使用 gpt-4o

# Anthropic Claude (可选，如果使用 Claude)
ANTHROPIC_API_KEY=your_anthropic_api_key
ANTHROPIC_MODEL=claude-3-opus-20240229  # 可选，默认使用 claude-3-opus

# 注意：
# 1. 如果只使用 OpenAI，只需配置 OPENAI_API_KEY
# 2. 如果只使用 Claude，只需配置 ANTHROPIC_API_KEY
# 3. 如果两个都配置，可以在界面中切换使用